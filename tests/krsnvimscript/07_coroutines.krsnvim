-- Test 07: Lua Coroutines and Generators
print("Starting coroutine test...")

local gen = coroutine.create(function(max)
  for i = 1, max do
    coroutine.yield(i * 10)
  end
end)

local ok, vals = coroutine.resume(gen, 3)
print("Coroutine yielded:", vals)
print("Coroutine test completed.")
